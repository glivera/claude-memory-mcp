#!/bin/bash
docker compose down "$@"
# .env is legacy: bin/up no longer generates it (file-less, doppler run
# injects secrets directly). This just cleans up any leftover file from
# before that change.
rm -f .env
